// zstd.no — zstd compression utility (delegates to system zstd)
// Usage: zstd [OPTIONS] FILE...
zstd-cmd = () {
    argc = args()
    cmd = 'zstd'
    i <- [2..argc): {
        cmd = cmd - ' ' - arg(i)
    }
    process.process-system(cmd)
}
