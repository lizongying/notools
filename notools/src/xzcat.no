// xzcat.no — xzcat utility: decompress to stdout (delegates to system xzcat)
// Usage: xzcat FILE...
xzcat-cmd = () {
    argc = args()
    cmd = 'xzcat'
    i <- [2..argc): {
        cmd = cmd - ' ' - arg(i)
    }
    process.process-system(cmd)
}
