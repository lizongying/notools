// nproc.no — nproc utility
// Print the number of available processors
// Usage: nproc [--all]

nproc-run = () {
    argc = args()

    i <- [2..argc): {
        a = arg(i)
        a == '--all' -> {}
    }

    n = os.num-cpu()
    n <= 0 -> n = 1
    print(n)
}
