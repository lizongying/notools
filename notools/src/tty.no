// tty.no — tty utility
// Print the file name of the terminal connected to standard input
// Usage: tty

tty-run = () {
    name = os.ttyname(0)
    name.len > 0 -> {
        print(name)
        return
    }
    eprint('not a tty')
    os.exit(1)
}
