// unzstd.no — unzstd decompression utility (delegates to system unzstd)
// Usage: unzstd [OPTIONS] FILE...
unzstd-cmd = () {
    argc = args()
    cmd = 'unzstd'
    i <- [2..argc): {
        cmd = cmd - ' ' - arg(i)
    }
    process.process-system(cmd)
}
