// chroot.no — chroot utility (delegates to system chroot)
// Usage: chroot NEWROOT [COMMAND [ARGS]...]
chroot-cmd = () {
    argc = args()
    cmd = 'chroot'
    i <- [2..argc): {
        cmd = cmd - ' ' - arg(i)
    }
    process.process-system(cmd)
}
