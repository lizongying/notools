// unlzma.no — unlzma decompression utility (delegates to system unxz --format=lzma)
// Usage: unlzma [OPTIONS] FILE...
unlzma-cmd = () {
    argc = args()
    cmd = 'unxz --format=lzma'
    i <- [2..argc): {
        cmd = cmd - ' ' - arg(i)
    }
    process.process-system(cmd)
}
