// bzcat.no — bzcat utility: decompress to stdout (delegates to system bzcat)
// Usage: bzcat FILE...
bzcat-cmd = () {
    argc = args()
    cmd = 'bzcat'
    i <- [2..argc): {
        cmd = cmd - ' ' - arg(i)
    }
    process.process-system(cmd)
}
