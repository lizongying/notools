// lzcat.no — lzcat utility: decompress to stdout (delegates to system xzcat --format=lzma)
// Usage: lzcat FILE...
lzcat-cmd = () {
    argc = args()
    cmd = 'xz --format=lzma -dc'
    i <- [2..argc): {
        cmd = cmd - ' ' - arg(i)
    }
    process.process-system(cmd)
}
