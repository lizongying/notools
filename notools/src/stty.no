; stty.no — stty utility (delegates to system stty)
; Usage: stty [OPTIONS] [SETTINGS]
stty-cmd = () {
    argc = args()
    cmd = 'stty'
    i <- [2..argc): {
        cmd = cmd - ' ' - arg(i)
    }
    process.process-system(cmd)
}
