// lzma.no — lzma compression utility (delegates to system xz --format=lzma)
// Usage: lzma [OPTIONS] FILE...
lzma-cmd = () {
    argc = args()
    cmd = 'xz --format=lzma'
    i <- [2..argc): {
        cmd = cmd - ' ' - arg(i)
    }
    process.process-system(cmd)
}
