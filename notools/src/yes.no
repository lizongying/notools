// yes.no — yes utility
// Repeatedly output a string until killed
// Usage: yes [string...]
//   No args: outputs 'y' repeatedly
//   Multiple args: outputs them joined by spaces

yes-run = () {
    argc = args()
    s = 'y'
    argc >= 3 -> {
        s = arg(2)
        i <- [3..argc): {
            s = s - ' ' - arg(i)
        }
    }
    {
        print(s)
    } ()
}
