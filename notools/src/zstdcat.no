// zstdcat.no — zstdcat utility: decompress to stdout (delegates to system zstdcat)
// Usage: zstdcat FILE...
zstdcat-cmd = () {
    argc = args()
    cmd = 'zstdcat'
    i <- [2..argc): {
        cmd = cmd - ' ' - arg(i)
    }
    process.process-system(cmd)
}
