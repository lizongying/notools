// stdbuf.no — stdbuf utility (delegates to system stdbuf)
// Usage: stdbuf OPTION... COMMAND
stdbuf-cmd = () {
    argc = args()
    cmd = 'stdbuf'
    i <- [2..argc): {
        cmd = cmd - ' ' - arg(i)
    }
    process.process-system(cmd)
}
