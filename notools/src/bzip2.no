// bzip2.no — bzip2 compression utility (delegates to system bzip2)
// Usage: bzip2 [OPTIONS] FILE...
bzip2-cmd = () {
    argc = args()
    cmd = 'bzip2'
    i <- [2..argc): {
        cmd = cmd - ' ' - arg(i)
    }
    process.process-system(cmd)
}
