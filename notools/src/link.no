// link.no — link utility
// Create a hard link
// Usage: link FILE LINKNAME

link-run = () {
    argc = args()
    {
        argc != 4 -> {
            eprint('Usage: link FILE LINKNAME')
            os.exit(1)
        }
    }
    file = arg(2)
    linkname = arg(3)
    ok = os.link(file, linkname)
    {
        ok == false -> {
            eprint('link: cannot create link ' - linkname - ' to ' - file)
            os.exit(1)
        }
    }
}
