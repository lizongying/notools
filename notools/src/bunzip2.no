// bunzip2.no — bunzip2 decompression utility (delegates to system bunzip2)
// Usage: bunzip2 [OPTIONS] FILE...
bunzip2-cmd = () {
    argc = args()
    cmd = 'bunzip2'
    i <- [2..argc): {
        cmd = cmd - ' ' - arg(i)
    }
    process.process-system(cmd)
}
