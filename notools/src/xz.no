// xz.no — xz compression utility (delegates to system xz)
// Usage: xz [OPTIONS] FILE...
xz-cmd = () {
    argc = args()
    cmd = 'xz'
    i <- [2..argc): {
        cmd = cmd - ' ' - arg(i)
    }
    process.process-system(cmd)
}
