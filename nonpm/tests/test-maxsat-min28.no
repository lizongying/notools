# /nonpm/src/semver

; compare inside a function
my-test-fn = () (c i64) {
    v1, ok1 = semver.parse('1.0.0-a')
    v2, ok2 = semver.parse('1.0.0-b')
    c = semver.compare(v1, v2)
}

main = () {
    c = my-test-fn()
    print('c=' - c.to-str())
}
