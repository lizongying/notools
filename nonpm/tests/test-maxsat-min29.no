# /nonpm/src/semver

; compare without pre-release (no heap fields)
my-test-fn = () (c i64) {
    v1, ok1 = semver.parse('1.0.0')
    v2, ok2 = semver.parse('1.0.1')
    c = semver.compare(v1, v2)
}

main = () {
    c = my-test-fn()
    print('c=' - c.to-str())
}
