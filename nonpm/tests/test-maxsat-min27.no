# /nonpm/src/semver

; Minimal: just parse two versions and compare
main = () {
    v1, ok1 = semver.parse('1.0.0-a')
    v2, ok2 = semver.parse('1.0.0-b')
    c = semver.compare(v1, v2)
    print('c=' - c.to-str())
}
